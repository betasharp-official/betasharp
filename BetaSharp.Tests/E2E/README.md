# E2E Test Slice (Headless)

Initial end-to-end coverage is intentionally small and focused on player join.

Run only the headless e2e tests:

`dotnet test --filter "Category=E2E&Mode=Headless"`

Run all e2e tests:

`dotnet test --filter "Category=E2E"`
